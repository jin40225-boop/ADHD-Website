import { X } from 'lucide-react'; import { useEffect } from 'react';
import type { ReactNode } from 'react';
export interface ModalProps { /** Shows or hides the dialog. */ open: boolean; /** Called for overlay click or Escape. */ onClose: () => void; /** Accessible dialog title. */ title: string; /** Dialog content. */ children: ReactNode; /** Optional action row. */ footer?: ReactNode; }
/** Accessible modal dialog that closes on overlay click and Escape. */
export function Modal({ open,onClose,title,children,footer }: ModalProps) { useEffect(()=>{if(!open)return;const handler=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose()};window.addEventListener('keydown',handler);return()=>window.removeEventListener('keydown',handler)},[open,onClose]); if(!open)return null; return <div className="ui-overlay" role="presentation" onMouseDown={event=>{if(event.target===event.currentTarget)onClose()}}><section className="ui-modal" role="dialog" aria-modal="true" aria-label={title}><header className="ui-modal__header"><strong>{title}</strong><button className="ui-icon-button" type="button" onClick={onClose} aria-label="關閉"><X /></button></header><div className="ui-modal__body">{children}</div>{footer ? <footer className="ui-modal__footer">{footer}</footer> : null}</section></div>; }
