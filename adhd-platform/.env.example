# 前端環境變數範例。複製為 .env（已在 .gitignore）後填入。
# anon key 受 RLS 保護，可安全放前端；service_role key 絕不放這裡。

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# 選填：覆寫部署 base path（預設 production 為 /ADHD-Website/）。
# 部署到 Cloudflare Pages / 根網域時設為 /
# VITE_BASE=/
